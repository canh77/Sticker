package com.lutech.stickerwhatsapp.database.dao

import androidx.lifecycle.LiveData
import androidx.room.*
import androidx.room.OnConflictStrategy.Companion.REPLACE
import com.lutech.stickerwhatsapp.model.Sticker

interface StickerDao {
//    @Query("SELECT * FROM sticker")
//    fun getSticker(): List<Sticker>
//
//    @Insert(onConflict = REPLACE)
//    fun insertAll(sticker: List<Sticker>)
//
//    @Insert(onConflict = REPLACE)
//    fun insert(sticker: Sticker)

}